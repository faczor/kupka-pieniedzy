# Kupka Pieniędzy — wersja aplikacji
# Jedyne źródło prawdy wersjonowania dla Android i iOS.
#
# VERSION_NAME: wersja widoczna dla użytkownika (np. 1.0, 1.2.3) — Apple "Version".
# VERSION_CODE: build number, MUSI rosnąć przy każdym wydaniu/uploadzie — Apple "Build".
#
# Użycie:
# - Edytuj ten plik, żeby bumpnąć wersję (przed deployem).
# - Android: Gradle czyta automatycznie (androidApp/build.gradle.kts).
# - iOS: ./scripts/ios/deploy-ios.sh synchronizuje to do Config.xcconfig przed archiwizacją.

VERSION_NAME=1.0.3
VERSION_CODE=5
